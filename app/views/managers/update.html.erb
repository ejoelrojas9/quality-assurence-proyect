
<div class="container">
  <div class="content-header mt-5">
    <% if current_admin.present? %>
      <a href="<%= root_path -%>" class="p-3" ><i class="fa fa-ban fa-fw pull-right"></i> Home</a>
      <a href="<%= managers_path %>" class="p-3"><i class="fa fa-ban fa-fw pull-righ"t"></i>Managers</a>
      <a href="<%= destroy_admin_session_path -%>" class="p-3"><i class="fa fa-ban fa-fw pull-right"></i> Logout</a>
    <% end %>
    <div class="container mt-3">
      <table>
        <tr>
        </tr>
      </table>
    </div>
    <div class="header-section mt-3">
      <h3>Access de information of the <%= @manager.name %></h3>
    </div>
    <div class="block full block-all-noboder table-responsive-form-control mt-3">
      
    </div>
  </div>
</div>