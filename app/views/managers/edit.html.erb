
<div class="container bg-light">
  <div class="content-header mt-5 p-5">
    <% if current_admin.present? %>
      <a href="<%= managers_path %>" class="p-3"><i class="fa fa-ban fa-fw pull-righ"t"></i>Home</a>
      <a href="<%= destroy_admin_session_path -%>" class="p-3"><i class="fa fa-ban fa-fw pull-right"></i> Logout</a>
    <% end %>
    <div><h3 class="text-center my-5 heading text-primary">Manager Form</h3></div>
      <%= render 'form' %>
  </div>
</div>

